Stream：
前端SSc页面：添加 体检方式、体检时间

后端：entity添加两个字段
		导出代码添加两个字段的导出

注：体检方式为字典，需配置dicCode。code来源：↓

 

代码：
1、	web:  SSCStartEditList.vue
{
          prop: { db_column_name: 'examinationType', table: 'OBM_EXAMINATION_INFO' },
          title: '体检方式',
          key: 'examinationType',
          dictCode: 'TJFS',
          type: JVXETypes.select,
          align: 'center',
          width: '120px',
        },
        {
          prop: { db_column_name: 'examinationTime', table: 'OBM_EXAMINATION_INFO' },
          title: '体检日期',
          key: 'examinationTime',
          type: JVXETypes.select,
          align: 'center',
          width: '120px',
        },

2、	SscStartExportVO.java  导出
/**
 * 体检方式
 */
@Excel(name = "体检方式", width = 15, dicCode = "TJFS")
@Dict(dicCode = "TJFS")
@ApiModelProperty(value = "体检方式")
private java.lang.String examinationType;
/**
 * 体检日期
 */
@Excel(name = "体检日期", width = 15, format = "yyyy-MM-dd")
@JsonFormat(timezone = "GMT+8",pattern = "yyyy-MM-dd")
@DateTimeFormat(pattern="yyyy-MM-dd")
@ApiModelProperty(value = "体检日期")
private java.lang.String examinationTime;

3、	ViObmEimInfo.java    数据库entity
/**
 * 体检方式
 */
@Excel(name = "体检方式", width = 15, dicCode = "TJFS")
@Dict(dicCode = "TJFS")
@ApiModelProperty(value = "体检方式")
private java.lang.String examinationType;
/**
 * 体检日期
 */
@Excel(name = "体检日期", width = 15, format = "yyyy-MM-dd")
@JsonFormat(timezone = "GMT+8",pattern = "yyyy-MM-dd")
@DateTimeFormat(pattern="yyyy-MM-dd")
@ApiModelProperty(value = "体检日期")
private java.lang.String examinationTime;

